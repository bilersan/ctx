version: "2"

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - unused
    - ineffassign
    - goconst
    - gosec
    - unconvert
    - gocritic

  settings:
    govet:
      enable:
        - shadow

  exclusions:
    paths:
      - vendor
      - dist
      - site
    rules:
      # Test files use literal strings for clarity
      - linters: [goconst]
        path: "_test\\.go"
      # Test files read known fixtures, not user input
      - linters: [gosec]
        path: "_test\\.go"
        text: "G304"

run:
  timeout: 5m

output:
  formats:
    text:
      colors: true
